global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets:
          - prometheus:9090

  # Backend /metrics is planned. Keep Prometheus internal and add this target
  # when application metrics are implemented.
  # - job_name: backend
  #   static_configs:
  #     - targets:
  #         - backend:8000
